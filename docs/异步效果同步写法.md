# 异步效果的同步写法（`fx`）

教学层：用 **看起来同步** 的 `Cont.withEnv` 步骤管道书写业务，把 wait / 网络连接 / 点击等 **异步效果** 表达成 `Coro.yield` 请求，由外部解释器兑现。

| 角色 | 模块 | 职责 |
|------|------|------|
| 业务管道 | `Cont.withEnv` | 按名顺序 `>>`；步骤里调用 `fx.*` |
| 效果原语 | [`src/fx.lua`](../src/fx.lua) | `wait` / `connect` / `click` → yield `{ kind=... }` |
| CPS 挂起 | [`src/coro.lua`](../src/coro.lua) | `Coro.yield` / `Coro.run`（答案 `Done` \| `Yielded`） |
| 解释器 | `fx.run(ma, handlers?)` | 按 `kind` 分派；默认可 mock，测试可覆盖 |

## 不是什么

- **不是**真实网络或 UI；默认 handlers 只打印日志并返回 mock 表。
- **不是** Lua 原生 `coroutine` / 已放弃的 `perform`/`runDo`。`Coro.yield` 是 Cont 编码的挂起，业务请走 `fx` / `Coro`，不要当原生协程 API。
- `Cont.withEnv` 只组织 `>>`；挂起与恢复仍由 Coro / `fx.run` 完成。

## 最小形状

```lua
package.path = "src/?.lua;" .. package.path
local Cont = require("cont")
local fx = require("fx")

local pipe = Cont.withEnv(function(_ENV)
  function step(_)
    return fx.wait(0.05) >> function(_)
      return fx.click("ok")
    end
  end
end)

-- 默认 mock（wait 会短 busy-wait）；测试可传瞬时 handlers
local result = fx.run(pipe(nil))
```

`fx.wait(seconds)` yield `{ kind="wait", seconds }`，resume 后业务得到 `true`。  
`fx.connect(host, opts?)` / `fx.click(target)` resume 值即结果表。

自定义 handlers（瞬时、记事件）示例见 [`examples/fx_custom_handlers.lua`](../examples/fx_custom_handlers.lua)。

## 与属性叠用

`__Require__` / `__Trace__` 等包装的是 Cont 步骤；步骤体内仍可 `fx.wait`。拒绝时不进入 yield。见 [`examples/fx_with_attrs.lua`](../examples/fx_with_attrs.lua)。

## 可执行示例

| 文件 | 演示 |
|------|------|
| [`examples/fx_wait_click_flow.lua`](../examples/fx_wait_click_flow.lua) | open_ui → wait → click → wait → connect → done；默认 mock 时间线 |
| [`examples/fx_custom_handlers.lua`](../examples/fx_custom_handlers.lua) | 同管道 + 瞬时 handlers + 事件日志断言 |
| [`examples/fx_with_attrs.lua`](../examples/fx_with_attrs.lua) | `__Require__` + `__Trace__` 包住 `fx.wait` |

```bash
lua examples/fx_wait_click_flow.lua
lua examples/fx_custom_handlers.lua
lua examples/fx_with_attrs.lua
```

更底层的 withEnv + Coro 混用见 [`examples/cont_env_coro_mix.lua`](../examples/cont_env_coro_mix.lua) 与 [Cont环境组合.md](./Cont环境组合.md)。
