# 工程可用验收清单

目标宿主：**Unity + Lua 5.3**。对照「演示级 → 工程级」验收条；  
状态以本仓库 `master` 为准（版本 **`0.2.2-eng`**，见 README / CHANGELOG）。

图例：✅ 已落地　🔶 部分　❌ 未做

---

## 1. 本轮 Acceptance

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | 本文档 | ✅ | [`docs/工程可用验收.md`](工程可用验收.md) |
| 2 | 效果注册表 | ✅ | [`src/fx_registry.lua`](../src/fx_registry.lua)；`fx.register` / `fx.unregister`；`GameSim:register` |
| 2a | 未知 kind → Failed | ✅ | session 返回结构化 Failed（含 `tag` / `effect_kind`），非静默 |
| 2b | 标准 kind 文档 | ✅ | `fx_registry.STANDARD_KINDS`：wait、wait_event、**wait_until**、when_all、when_any、fork、join、join_handles、with_timeout；演示 anim；遗留 connect / click |
| 3 | 实体绑定 | ✅ | [`src/fx_flow.lua`](../src/fx_flow.lua)；`fx.bind_entity`；`GameSim:start_flow` 经 `Flow.bind_entity` |
| 4 | 资源 bracket | ✅ | `fx.with_resource` / `Cont.bracket`；Done / Stopped / Failed 均 release；测试覆盖 |
| 5 | 轻量追踪 | ✅ | `opts.trace` / `fx.set_tracer`（默认关）；flow_start / yield / resume / fork / join / cancel / done\|failed\|stopped |
| 6 | CHANGELOG + 版本号 | ✅ | [`CHANGELOG.md`](../CHANGELOG.md)、README `0.2.2-eng` |
| 7 | `scripts/test_lua53.sh` | ✅ | 调用 `lua5.3 tests/run.lua` |
| 8 | 更新对接文档 P0/P1 | ✅ | [`工程对接与后续.md`](工程对接与后续.md)；wait_event 已非「规划中」 |
| 9 | Unity host 注释对齐 | ✅ | [`host/unity/LuaGameScheduler.lua`](../host/unity/LuaGameScheduler.lua) |
| 10 | 测试全绿 | ✅ | `lua5.3 tests/run.lua`；含 GameSim / dungeon_raid / FrameScheduler / wait_until |
| 11 | 截止时间向下传播 | ✅ | fork / when_all 继承；父超时取消子树；子可更紧 `with_timeout`；VirtualClock+GameSim 测 |
| 12 | 嵌套 bracket 说明 | ✅ | `Cont.bracket` 注释 + 测：内层 release 先于外层 |
| 13 | `fx.wait_until` + FrameScheduler | ✅ | 每 tick/interval poll；`FrameScheduler.tick` / `GameSim.tick`；VirtualClock 经 schedule 模拟 |

---

## 2. 分层能力总览

| 能力 | 状态 | 入口 |
|------|------|------|
| 游戏时间 Scheduler | ✅ | `scheduler.lua` / GameSim / Unity host |
| cancel 清 timer + 忽略迟到回调 | ✅ | fx_sched + GameSim / LuaGameScheduler |
| wait 走 scheduler | ✅ | `opts.scheduler` |
| wait_event | ✅ | `fx.wait_event` + `GameSim:emit` |
| 效果注册（全局 + per-sim） | ✅ | `fx.register`、`GameSim:register` |
| 未知 kind Failed | ✅ | `Registry.unknown_error` |
| 实体销毁 → cancel + finally | ✅ | `fx.bind_entity` / `GameSim:destroy_entity` |
| 资源作用域 bracket | ✅ | `fx.with_resource` / `Cont.bracket` |
| 轻量 flow 追踪 | ✅ | `opts.trace` / `fx.set_tracer` |
| 截止时间向下传播 | ✅ | `with_timeout` / `opts.timeout\|deadline` → fork 继承；超时递归 Stopped + finally |
| FrameScheduler / wait_until | ✅ | `scheduler.FrameScheduler`；`fx.wait_until`；GameSim.`schedule_poll` |
| 有界 channel / 监督重启 | ❌ | 可选 P2（不阻塞「工程可用」核心闭环） |
| 墙钟 wait_real | ❌ | 默认不做 |

---

## 3. 距「工程可用标准已达标」还剩什么

**以本清单 §1 Acceptance（含截止继承、wait_until / FrameScheduler）为准：核心工程闭环已齐，可视为已达标。**

仍属**可选**、不阻塞达标：

| 项 | 说明 |
|----|------|
| 有界 channel / mailbox | 多 flow 协作；按项目需要再做 |
| 监督式重启 | 子任务失败策略；按项目需要再做 |
| 墙钟 `wait_real` | 网络/SDK；默认业务禁用 |

演示回退：无 `opts.scheduler` 时 `wait` / `wait_until` 仍可能走 `busy_wait`——**仅 demo / 单测便利，工程请接 GameSim / FrameScheduler / Unity host**。

---

## 4. 回归命令

```bash
./scripts/test_lua53.sh
# 或
lua5.3 tests/run.lua

lua5.3 examples/game_sim_flow.lua
lua5.3 examples/dungeon_raid/main.lua
```

期望：`All tests passed.`

---

## 5. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-09-21 | 初稿：对照 0.2.0-eng Acceptance |
| 2026-09-21 | P1：截止时间向下传播；嵌套 bracket 注记 |
| 2026-09-21 | 0.2.2-eng：wait_until + FrameScheduler；声明核心闭环已达标 |
