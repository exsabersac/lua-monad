# 工具速查（一页）

目标：**Lua 5.3**，在**仓库根**执行。版本 **`0.2.16-eng`**。详解见 [性能与工具](性能与工具.md) / [工具套件收口](工具套件收口.md) / [`tools/README.md`](../tools/README.md)。

共享场景：[`tools/scenarios/`](../tools/scenarios/)（`sync_seq` / `wait_vc` / `lane_pair` / `chan_ping` / `supervise_once`）— `profile_flow` / `trace_export` / `bench_cont_fx` 复用，**无 Tab DSL**。

---

## 一键 CI

```bash
./scripts/ci_tools.sh
ALLOW_BENCH_REGRESSION=1 ./scripts/ci_tools.sh   # bench 回归 soft-fail
PROFILE=1 ./scripts/ci_tools.sh                  # profile_flow N=20（默认 --smoke N=1）
./scripts/test_lua53.sh                          # 仅单元测试
lua5.3 tests/run.lua
```

顺序：`test_lua53` → `flow_doctor --ci` → `bench_compare --ci` → `alloc_hotspot` smoke → `profile_flow`。

---

## 性能基准

```bash
# 热路径粗测（默认 N=20000；--json / --filter / --alloc）
lua5.3 tools/bench_cont_fx.lua
lua5.3 tools/bench_cont_fx.lua 50000
lua5.3 tools/bench_cont_fx.lua --json
lua5.3 tools/bench_cont_fx.lua --filter lane
lua5.3 tools/bench_cont_fx.lua --alloc --json --filter 'Cont >>'
lua5.3 tools/bench_cont_fx.lua --help

# 对照基线 / 写基线 / CI 回归（默认阈值 25%）
lua5.3 tools/bench_compare.lua
lua5.3 tools/bench_compare.lua --write-baseline
lua5.3 tools/bench_compare.lua --ci
lua5.3 tools/bench_compare.lua --ci --threshold 0.25
lua5.3 tools/bench_compare.lua --ci --all
lua5.3 tools/bench_compare.lua --help

# JSON → Markdown 摘要
lua5.3 tools/bench_summary.lua
lua5.3 tools/bench_summary.lua --out tools/bench_summary.md
lua5.3 tools/bench_cont_fx.lua --json | lua5.3 tools/bench_summary.lua --stdin
lua5.3 tools/bench_summary.lua --from /tmp/bench.ndjson --out tools/bench_summary.md
lua5.3 tools/bench_summary.lua --help

# Cont >> 分配热点
lua5.3 tools/alloc_hotspot.lua
lua5.3 tools/alloc_hotspot.lua 10000
lua5.3 tools/alloc_hotspot.lua --map
lua5.3 tools/alloc_hotspot.lua --json 5000
lua5.3 tools/alloc_hotspot.lua --help
```

---

## 场景打点 / Trace

```bash
# wrapping tracer：os.clock / 墙钟；按 kind(+step) 汇总 top
lua5.3 tools/profile_flow.lua              # 默认 N=20；场景含 supervise once
lua5.3 tools/profile_flow.lua 50
lua5.3 tools/profile_flow.lua --smoke      # N=1，CI 默认
lua5.3 tools/profile_flow.lua --json
lua5.3 tools/profile_flow.lua --json --out tools/profile_out.json
lua5.3 tools/profile_flow.lua --filter lane --top 8
lua5.3 tools/profile_flow.lua --filter sync_seq
lua5.3 tools/profile_flow.lua --help

# trace 事件 → stdout
lua5.3 tools/trace_dump.lua
lua5.3 tools/trace_dump.lua --json
lua5.3 tools/trace_dump.lua --lane --chan
lua5.3 tools/trace_dump.lua --wait 0.05
lua5.3 tools/trace_dump.lua --help

# trace 事件 → JSON 文件（默认 tools/trace_out.json）
lua5.3 tools/trace_export.lua
lua5.3 tools/trace_export.lua --lane --chan
lua5.3 tools/trace_export.lua --out /tmp/trace.json
lua5.3 tools/trace_export.lua --lane --chan --out tools/trace_out.json --stdout
lua5.3 tools/trace_export.lua --wait 0.05 --help
```

---

## 诊断 / 其它

```bash
# 误配置检查（未知 kind / wait 无 scheduler / STANDARD_KINDS）
lua5.3 tools/flow_doctor.lua
lua5.3 tools/flow_doctor.lua --kinds
lua5.3 tools/flow_doctor.lua --ci
lua5.3 tools/flow_doctor.lua --help

# @mdo 预处理
lua5.3 tools/run_mdo.lua examples/do_maybe_foo.mdo
```

---

## 共享场景（`tools/scenarios/`）

| id | 展示名 | 用途 |
|----|--------|------|
| `sync_seq` | sync seq | 纯同步 `fx.seq`×3 |
| `wait_vc` | wait VirtualClock | `fx.wait` + unit |
| `lane_pair` | lane | 命名 lane + join |
| `chan_ping` | chan | send/recv ping |
| `supervise_once` | supervise once | fail→ok 监督重启 |

```lua
package.path = "src/?.lua;tools/?.lua;tools/?/init.lua;" .. package.path
local Scenarios = require("scenarios")
local ma = Scenarios.by_id.chan_ping.build({ msg = "x" })
```

详见 [`tools/scenarios/README.md`](../tools/scenarios/README.md)。
